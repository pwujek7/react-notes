import React from 'react';
import ReactDOM from 'react-dom';

const ROOT = document.getElementById('root');

const App = () => (
  <div>
    <h1>React notes project</h1>
  </div>
);

ReactDOM.render(<App />, ROOT);
